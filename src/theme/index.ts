import { colors, motion, radii, shadows, spacing, typography } from './tokens';

export const theme = {
  colors,
  spacing,
  radii,
  typography,
  shadows,
  motion,
};

export type Theme = typeof theme;
